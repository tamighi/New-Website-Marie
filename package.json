{
  "name": "root",
  "scripts": {
    "build": "lerna run build",
    "start:prod": "lerna run start:prod --scope=backend"
  },
  "devDependencies": {
    "lerna": "^6.4.1"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
