import { Header, MainCard } from "components";

const Dashboard = () => {
  return (
    <MainCard>
      <Header>
        <h2>Dashboard</h2>
      </Header>
    </MainCard>
  );
};

export default Dashboard;
