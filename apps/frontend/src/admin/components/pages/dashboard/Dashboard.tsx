import { ResponsiveGrid } from "lib";

export const Dashboard = () => {
  return (
    <ResponsiveGrid container>
      <ResponsiveGrid small={12}>
        <h2>Dashboard</h2>
      </ResponsiveGrid>
    </ResponsiveGrid>
  );
};
